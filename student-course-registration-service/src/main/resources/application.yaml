spring:
  application:
    name: student.course.registration.service

  datasource:
    url: jdbc:postgresql://localhost:5432/student_course_registration_db
    username: postgres
    password: sidemenxix
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8080

